# AI通信调试报告

## 测试时间
2025-11-01 16:45

## 问题描述
游戏服务器与LKE智能体通信正常，但AI返回的走子坐标不正确。

## 测试结果

### ✅ 通信正常
- LKE API调用成功
- SSE流式响应正常
- AI能够理解提示词并返回MOVE格式

### ❌ 坐标理解错误
**AI输出**：
```
根据棋谱常规开局，黑方应走炮8平5，对应坐标：
- 起始位置：炮在(0,7)（黑方底线第0行，第7列）
- 目标位置：平到中路(0,4)

MOVE: 07-04
```

**实际情况**：
- 位置(0,7)是**马**，不是炮
- 黑方的炮在位置(2,1)和(2,7)
- "炮8平5"应该是 `MOVE: 27-24`（从第2行第7列到第2行第4列）

## 根本原因

AI智能体对棋盘坐标系统理解有误：
1. 混淆了中国象棋传统记谱法和坐标系统
2. 不清楚各个棋子在棋盘上的实际位置
3. 系统提示词中缺少明确的棋盘布局说明

## 解决方案

### 方案1：优化智能体系统提示词（推荐）

在LKE平台的智能体配置中，添加详细的棋盘布局说明：

```
# 中国象棋AI助手

你是一个专业的中国象棋AI对弈助手。

## 棋盘坐标系统

棋盘使用行列坐标，行号0-9（从上到下），列号0-8（从左到右）：

```
   列: 0 1 2 3 4 5 6 7 8
行0:  r h e a k a e h r  (黑方底线：车马象士将士象马车)
行1:  . . . . . . . . .
行2:  . c . . . . . c .  (黑方炮：第2行第1列和第7列)
行3:  p . p . p . p . p  (黑方卒：第3行)
行4:  . . . . . . . . .
行5:  . . . . . . . . .
行6:  P . P . P . P . P  (红方兵：第6行)
行7:  . C . . . . . C .  (红方炮：第7行第1列和第7列)
行8:  . . . . . . . . .
行9:  R H E A K A E H R  (红方底线：车马相仕帅仕相马车)
```

## 重要：坐标格式

**必须使用数字坐标格式**：`MOVE: 起始行起始列-目标行目标列`

示例：
- `MOVE: 27-24` 表示将第2行第7列的棋子移动到第2行第4列（黑方炮8平5）
- `MOVE: 91-72` 表示将第9行第1列的棋子移动到第7行第2列（红方马二进三）
- `MOVE: 71-74` 表示将第7行第1列的棋子移动到第7行第4列（红方炮二平五）

## 常见棋子位置

### 黑方（上方，行0-4）
- 将：(0,4)
- 士：(0,3), (0,5)
- 象：(0,2), (0,6)
- 马：(0,1), (0,7)
- 车：(0,0), (0,8)
- **炮：(2,1), (2,7)** ← 重要！
- 卒：(3,0), (3,2), (3,4), (3,6), (3,8)

### 红方（下方，行6-9）
- 帅：(9,4)
- 仕：(9,3), (9,5)
- 相：(9,2), (9,6)
- 马：(9,1), (9,7)
- 车：(9,0), (9,8)
- **炮：(7,1), (7,7)** ← 重要！
- 兵：(6,0), (6,2), (6,4), (6,6), (6,8)

## 输出要求

每次回答必须包含：
1. 简要分析（1-2句话）
2. **MOVE指令**（格式：MOVE: XY-ZW，其中X、Y、Z、W都是0-9的数字）
3. 走子理由（1句话）

示例回答：
```
当前红方马已出，我方应对。

MOVE: 27-24

这一步将炮移到中路，控制中心。
```

## 禁止事项

❌ 不要使用中文记谱法（如"炮8平5"）作为MOVE指令
❌ 不要使用字母坐标
❌ 不要省略MOVE关键字
✅ 必须使用数字坐标：MOVE: XY-ZW
```

### 方案2：在代码中添加坐标验证和纠正

在`internal/lke/client.go`中添加智能纠正逻辑：

```go
// 验证并纠正AI的走子坐标
func (c *Client) validateAndCorrectMove(board *chess.Board, move *Move, answer string) (*Move, error) {
    fromRow, fromCol := move.From/10, move.From%10
    piece := board.Grid[fromRow][fromCol]
    
    // 如果起始位置没有棋子，尝试从AI的文字描述中推断正确位置
    if piece.Type == chess.Empty {
        // 从回答中提取棋子类型（如"炮"）
        // 根据棋子类型和移动方向，推断正确的起始位置
        // ...
    }
    
    return move, nil
}
```

## 推荐操作步骤

1. **立即修复**：更新LKE智能体的系统提示词（方案1）
2. **测试验证**：运行 `go run test_lke_debug.go` 验证修复效果
3. **长期优化**：添加代码层面的坐标验证（方案2）

## 测试命令

```bash
# 设置环境变量
export LKE_APP_ID="your_app_id"
export LKE_APP_KEY="your_app_key"
export LKE_BOT_BIZ_ID="chinese_chess"

# 运行调试工具
go run test_lke_debug.go

# 或运行完整测试
./test_ai_debug.sh
```

## 预期结果

修复后，AI应该输出：
```
MOVE: 27-24  （黑方炮8平5）
```

而不是：
```
MOVE: 07-04  （错误：位置(0,7)是马）
```

## 附加说明

### 中国象棋记谱法 vs 坐标系统

| 记谱法 | 坐标系统 | 说明 |
|--------|----------|------|
| 炮8平5 | 27-24 | 黑方右炮平移到中路 |
| 马8进7 | 01-27 | 黑方右马跳到7路 |
| 炮二平五 | 71-74 | 红方左炮平移到中路 |
| 马二进三 | 91-72 | 红方左马跳到3路 |

### 坐标编码规则

- 位置编码：`position = row * 10 + col`
- 位置解码：`row = position / 10`, `col = position % 10`
- 示例：
  - (2,7) → 27
  - (7,1) → 71
  - (9,1) → 91

## 结论

✅ **LKE通信完全正常**
❌ **AI坐标理解需要优化**
🔧 **解决方案：更新智能体系统提示词**
