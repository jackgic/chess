# AI交互问题修复说明

## 问题描述

虽然已经按照 `LKE_SYSTEM_PROMPT_OPTIMIZED.md` 更新了LKE平台的系统提示词，但AI在实际对弈中仍然存在以下问题：

1. **坐标理解错误**：AI可能输出不合法的走子坐标
2. **规则理解不足**：AI对某些棋子（如马、象）的移动规则理解有误
3. **提示词冲突**：用户提示词与系统提示词可能存在冲突或重复

## 已实施的修复

### 1. 优化用户提示词（代码层面）

**修改文件**：`internal/game/manager.go`

**改进内容**：
- ✅ 添加棋盘可视化表示（不仅仅是FEN格式）
- ✅ 更清晰的格式说明和示例
- ✅ 强调坐标系统的重要性
- ✅ 提供更结构化的输出要求

**新的提示词格式**：
```
# 当前对局状态

## 棋盘布局（FEN格式）
[FEN字符串]

## 当前棋盘可视化
```
   列: 0 1 2 3 4 5 6 7 8
行0:  r h e a k a e h r  (黑方底线)
行1:  . . . . . . . . .
行2:  . c . . . . . c .  (黑方炮)
...
```

## 对局信息
- 当前轮到：黑方/红方
- 你执：黑方/红方
- 历史走子：[走子列表]

## 你的任务
请分析当前局面，选择一个合法的走子。

## 输出要求
你必须严格按照以下格式输出：

第一行：简要分析（1句话）
第二行：MOVE: 起始行起始列-目标行目标列
第三行：走子理由（1句话）

**重要提示**：
- 坐标格式必须是4位数字，例如：MOVE: 27-24（从第2行第7列到第2行第4列）
- 行号范围：0-9（0是最上方，9是最下方）
- 列号范围：0-8（0是最左侧，8是最右侧）
- 黑方在上方（行0-4），红方在下方（行5-9）
```

### 2. 添加棋盘可视化方法

**修改文件**：`internal/chess/board.go`

**新增方法**：`ToString()`

**功能**：将棋盘转换为易读的可视化格式，帮助AI更好地理解当前棋盘布局。

```go
func (b *Board) ToString() string {
    // 生成类似以下格式的输出：
    //    列: 0 1 2 3 4 5 6 7 8
    // 行0:  r h e a k a e h r  (黑方底线)
    // 行1:  . . . . . . . . .
    // ...
}
```

## 验证步骤

### 步骤1：重新编译和启动服务

```bash
# 停止现有服务
pkill -f chinese-chess-ai

# 重新编译
go build -o chinese-chess-ai cmd/server/main.go

# 启动服务
./start.sh
```

### 步骤2：创建新游戏并测试

```bash
# 1. 创建新游戏
curl -X POST http://localhost:8090/api/game/new \
  -H "Content-Type: application/json" \
  -d '{"playerColor": 1}'

# 记录返回的游戏ID
GAME_ID="返回的游戏ID"

# 2. 玩家走一步（例如：炮二平五）
curl -X POST http://localhost:8090/api/game/move \
  -H "Content-Type: application/json" \
  -d "{\"gameId\": \"$GAME_ID\", \"fromRow\": 7, \"fromCol\": 1, \"toRow\": 7, \"toCol\": 4}"

# 3. 请求AI走子
curl -X POST http://localhost:8090/api/game/$GAME_ID/ai-move \
  -H "Content-Type: application/json"
```

### 步骤3：查看日志

```bash
# 查看实时日志
tail -f logs/chinese-chess-ai.log

# 重点关注以下日志：
# [Game] 发送提示词给AI:
# [LKE] AI完整回答:
# [LKE] 提取到的走子字符串:
# [Game] 解析到的走子: from=X, to=Y
# [Game] 验证走子: 从(X,Y)到(Z,W)
```

### 步骤4：检查AI响应

**成功的标志**：
- ✅ AI输出包含 `MOVE: XY-ZW` 格式的走子指令
- ✅ 坐标是4位数字（如 `27-24`）
- ✅ 走子合法（日志显示"走子验证通过"）
- ✅ 游戏继续进行

**失败的标志**：
- ❌ 日志显示"起始位置没有棋子"
- ❌ 日志显示"AI走子不合法"
- ❌ AI输出的坐标不是4位数字
- ❌ 返回错误信息

## 常见问题排查

### 问题1：AI仍然输出错误坐标

**可能原因**：
1. LKE平台的系统提示词未正确保存
2. 使用了旧的会话（有历史对话缓存）
3. AI模型本身的理解能力限制

**解决方案**：
1. 重新检查LKE平台的系统提示词配置
2. 创建新的游戏（新的会话ID）
3. 在LKE平台尝试不同的模型

### 问题2：AI理解棋盘但走子不合法

**可能原因**：
1. AI对象棋规则理解不足（特别是马、象的移动规则）
2. 系统提示词中缺少详细的规则说明

**解决方案**：
1. 在LKE系统提示词中添加更详细的规则说明
2. 参考 `LKE_SYSTEM_PROMPT_OPTIMIZED.md` 中的"走子规则"部分
3. 考虑添加代码层面的走子建议功能

### 问题3：AI响应太慢

**可能原因**：
1. 提示词太长，AI处理时间增加
2. LKE服务负载高
3. 网络延迟

**解决方案**：
1. 简化提示词（保留核心信息）
2. 在LKE平台调整超时设置
3. 考虑使用更快的模型

## 进一步优化建议

### 1. 添加走子验证和建议功能

在AI返回不合法走子时，系统可以：
- 分析AI的意图（想移动哪个棋子）
- 提供该棋子的所有合法走子
- 选择最接近AI意图的合法走子

### 2. 优化历史走子格式

当前历史走子可能不够清晰，可以改进为：
```
历史走子：
1. 红方：炮二平五 (71-74)
2. 黑方：炮8平5 (27-24)
3. 红方：马二进三 (91-72)
```

### 3. 添加局面评估

在提示词中添加当前局面的简要评估：
- 双方棋子数量
- 是否有将军
- 是否有棋子被威胁

### 4. 使用Few-Shot学习

在系统提示词中添加几个完整的对局示例，帮助AI更好地理解：
```
示例对局：
初始局面 → 红方：炮二平五 (MOVE: 71-74) → 黑方：马8进7 (MOVE: 01-27) → ...
```

## 测试清单

- [ ] 服务成功重启
- [ ] 创建新游戏成功
- [ ] 玩家走子成功
- [ ] AI走子成功（返回合法坐标）
- [ ] 日志显示正确的解析过程
- [ ] 可以进行多回合对弈
- [ ] 前端界面显示正常

## 相关文档

- [LKE_SYSTEM_PROMPT_OPTIMIZED.md](./LKE_SYSTEM_PROMPT_OPTIMIZED.md) - LKE系统提示词配置
- [AI_DEBUG_REPORT.md](./AI_DEBUG_REPORT.md) - AI调试报告
- [DEBUG_GUIDE.md](./DEBUG_GUIDE.md) - 调试指南
- [FIX_GUIDE.md](./FIX_GUIDE.md) - 问题修复方案

## 总结

本次修复主要从**代码层面**优化了发送给AI的提示词：

1. ✅ 添加了棋盘可视化，让AI更直观地理解棋盘布局
2. ✅ 提供了更清晰的格式说明和输出要求
3. ✅ 强调了坐标系统的重要性

配合之前在**LKE平台**配置的系统提示词，应该能显著改善AI的走子质量。

如果问题仍然存在，请：
1. 检查LKE平台的系统提示词是否正确配置
2. 查看详细日志，分析AI的实际输出
3. 考虑使用不同的AI模型
4. 参考"进一步优化建议"部分进行改进
