# 中文棋谱快速参考

## 基本格式

```
棋子名 + 起始列 + 动作 + 目标列/步数
```

## 棋子分类

### 直线移动棋子
**车、炮、兵、卒**

- 数字表示**步数**
- 列不变（平移除外）

#### 示例
```
炮二平五  → 炮从第二列平移到第五列
车9进1   → 车从第9列向前移动1步
兵五进一  → 兵从第五列向前移动1步
炮8退2   → 炮从第8列向后移动2步
```

### 斜线移动棋子
**马、相、士**

- 数字表示**目标列**
- 行根据列差自动计算

#### 示例
```
马8进7   → 马从第8列移动到第7列（向前）
马二进三  → 马从第二列移动到第三列（向前）
相三进五  → 相从第三列移动到第五列（向前）
士5退4   → 士从第5列移动到第4列（向后）
```

## 马的日字规则

### 规则
- **列差1 → 行差2**
- **列差2 → 行差1**

### 示例

#### 黑方马8进7
```
起始位置: (0, 7)  [row 0, col 7]
目标列:   col 6   [黑方"7" = col 6]
列差:     1       [7 - 6 = 1]
行差:     2       [列差1 → 行差2]
目标位置: (2, 6)  [row 0 + 2 = 2]
```

#### 红方马二进三
```
起始位置: (9, 7)  [row 9, col 7，红方"二" = col 7]
目标列:   col 6   [红方"三" = col 6]
列差:     1       [7 - 6 = 1]
行差:     2       [列差1 → 行差2]
目标位置: (7, 6)  [row 9 - 2 = 7，红方向上]
```

## 列号对照表

### 红方（从右到左）
```
col:  0   1   2   3   4   5   6   7   8
红方: 九  八  七  六  五  四  三  二  一
```

### 黑方（从左到右）
```
col:  0   1   2   3   4   5   6   7   8
黑方: 1   2   3   4   5   6   7   8   9
```

## 动作说明

### 平
- **含义**：横向移动
- **行不变**，列改变
- **适用**：所有棋子

```
炮二平五  → row不变，col 7 → col 4
车9平8   → row不变，col 8 → col 7
```

### 进
- **含义**：向前移动
- **红方**：向上（row减小）
- **黑方**：向下（row增大）

```
车9进1   → 黑方车向下移动1步
兵五进一  → 红方兵向上移动1步
马8进7   → 黑方马向下移动到第7列
```

### 退
- **含义**：向后移动
- **红方**：向下（row增大）
- **黑方**：向上（row减小）

```
车1退2   → 黑方车向上移动2步
炮五退二  → 红方炮向下移动2步
马7退8   → 黑方马向上移动到第8列
```

## 完整对局示例

```
1. 炮二平五   (红方炮从col 7平移到col 4)
2. 马8进7    (黑方马从col 7移动到col 6)
3. 马二进三   (红方马从col 7移动到col 6)
4. 车9平8    (黑方车从col 8平移到col 7)
5. 车一平二   (红方车从col 8平移到col 7)
6. 炮2平5    (黑方炮从col 1平移到col 4)
```

## 常见错误

### ❌ 错误1：马的步数理解错误
```
错误理解: 马8进7 = 马向前移动7步
正确理解: 马8进7 = 马从第8列移动到第7列
```

### ❌ 错误2：列号方向错误
```
错误: 红方"一" = col 0
正确: 红方"一" = col 8 (红方从右到左)
```

### ❌ 错误3：行的方向错误
```
错误: 红方"进" = row增大
正确: 红方"进" = row减小 (红方向上)
```

## AI提示词建议

```
你是一个专业的中国象棋选手，棋力非凡，使用棋谱表示方法进行对弈

# 回答格式
MOVE: 炮二平五
MOVE: 马8进7

# 规则说明
- 红方使用中文数字：一二三四五六七八九
- 黑方使用阿拉伯数字：1 2 3 4 5 6 7 8 9
- 直线移动棋子（车炮兵）：数字表示步数
- 斜线移动棋子（马相士）：数字表示目标列
```

## 调试技巧

### 1. 查看棋盘坐标
```go
fmt.Printf("Grid[%d][%d] = %v\n", row, col, board.Grid[row][col])
```

### 2. 验证列号转换
```go
// 红方"二" → col 7
col := 9 - 2 = 7 ✅

// 黑方"8" → col 7
col := 8 - 1 = 7 ✅
```

### 3. 验证马的移动
```go
// 马8进7
fromCol := 7  // 黑方"8"
toCol := 6    // 黑方"7"
colDiff := 1  // 列差
rowDiff := 2  // 行差（列差1 → 行差2）
```

## 测试命令

```bash
# 运行完整测试
go run test_chinese_notation_complete.go

# 运行马的测试
go run test_ma8_debug.go

# 编译项目
go build -o chinese-chess-ai main.go

# 启动服务器
./start.sh

# 查看日志
tail -f logs/chinese-chess-ai.log | grep -E "\[Game\]|\[LKE\]"
```

## 相关文档

- [CHINESE_NOTATION_FIX_COMPLETE.md](CHINESE_NOTATION_FIX_COMPLETE.md) - 完整修复总结
- [CHESS_COLUMN_REFERENCE.md](CHESS_COLUMN_REFERENCE.md) - 列号参考卡片
- [QUICKSTART.md](QUICKSTART.md) - 快速开始指南
