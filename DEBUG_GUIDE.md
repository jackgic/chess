# 调试指南

## 问题诊断流程

### 1. 查看日志
```bash
# 查看实时日志
tail -f logs/chinese-chess-ai.log

# 查看错误日志
tail -f logs/chinese-chess-ai.error.log
```

### 2. 关键日志标记
- `[Game]` - 游戏管理器日志
- `[LKE]` - LKE客户端日志
- `[API]` - API处理日志

### 3. 常见问题排查

#### 问题1：AI走子不合法
**症状**：日志显示"起始位置没有棋子"或"AI走子不合法"

**排查步骤**：
1. 查看日志中的"AI完整回答"
2. 查看"提取到的走子字符串"
3. 查看"解析到的走子: from=X, to=Y"
4. 查看"验证走子: 从(X,Y)到(Z,W)"

**可能原因**：
- AI输出格式不正确
- 提示词中的示例被误解析
- 坐标转换错误

**解决方案**：
- 检查智能体提示词配置
- 确保提示词中没有具体的MOVE示例（如MOVE: 01-03）
- 使用抽象的格式说明（如MOVE: XY-ZW）

#### 问题2：解析到错误的MOVE指令
**症状**：AI输出`MOVE: 02-24`，但解析成`from=23, to=25`

**原因**：正则表达式匹配到了提示词中的示例，而不是AI的真实输出

**解决方案**：
- 已修复：现在使用`FindAllStringSubmatch`找到所有匹配项，返回最后一个
- 优化提示词：移除具体的MOVE示例

#### 问题3：AI响应超时
**症状**：请求超过60秒没有响应

**排查步骤**：
1. 检查网络连接
2. 查看LKE平台的智能体状态
3. 检查SecretId和SecretKey是否正确

**解决方案**：
- 增加超时时间（在client.go中修改）
- 优化提示词，减少AI思考时间
- 检查LKE平台的配额和限流

#### 问题4：AI输出包含提示词
**症状**：AI回复中包含了用户的提示词内容

**原因**：LKE的某些模型会回显用户输入

**解决方案**：
- 已修复：使用最后一个MOVE匹配项
- 在LKE平台调整模型参数

### 4. 调试技巧

#### 启用详细日志
在`internal/lke/client.go`中，所有关键步骤都有日志输出：
```go
log.Printf("[LKE] 开始解析AI回答，提取走子指令")
log.Printf("[LKE] AI完整回答: %s", answer)
log.Printf("[LKE] 提取到的走子字符串: %s", moveStr)
log.Printf("[LKE] 解析成功 - 从(%d,%d)到(%d,%d), from=%d, to=%d", ...)
```

#### 测试MOVE解析
使用`test_parse.go`测试解析逻辑：
```bash
go run test_parse.go
```

#### 手动测试LKE
使用`test/test_lke_direct.go`直接测试LKE连接：
```bash
go run test/test_lke_direct.go
```

### 5. 坐标系统说明

#### 棋盘坐标
```
   0 1 2 3 4 5 6 7 8  (列)
0  r h e a k a e h r  (黑方)
1  . . . . . . . . .
2  . c . . . . . c .
3  p . p . p . p . p
4  . . . . . . . . .
5  . . . . . . . . .
6  P . P . P . P . P
7  . C . . . . . C .
8  . . . . . . . . .
9  R H E A K A E H R  (红方)
(行)
```

#### 坐标转换
- 位置编码：`position = row * 10 + col`
- 位置解码：`row = position / 10`, `col = position % 10`
- 示例：位置(0,2)编码为2，位置(2,7)编码为27

#### MOVE格式
- `MOVE: 02-24` 表示从(0,2)移动到(2,4)
- `MOVE: 71-51` 表示从(7,1)移动到(5,1)

### 6. 最佳实践

#### 智能体提示词配置
1. 不要在提示词中包含具体的MOVE示例
2. 使用抽象的格式说明（如MOVE: XY-ZW）
3. 强调输出格式的重要性
4. 提供清晰的坐标系统说明

#### 代码修改
1. 修改提示词后，重启服务
2. 修改代码后，重新编译：`./start.sh`
3. 查看日志确认修改生效

#### 测试流程
1. 创建新游戏
2. 走一步棋
3. 查看日志中的AI回复和解析结果
4. 验证走子是否正确
5. 如有问题，根据日志定位原因

### 7. 快速修复清单

- [ ] 检查智能体提示词是否包含具体的MOVE示例
- [ ] 确认代码已更新到最新版本（使用FindAllStringSubmatch）
- [ ] 重启服务使修改生效
- [ ] 测试AI走子是否正常
- [ ] 查看日志确认解析结果

### 8. 联系支持

如果问题仍未解决，请提供：
1. 完整的日志文件
2. 智能体提示词配置
3. 问题复现步骤
4. AI的完整回复内容
