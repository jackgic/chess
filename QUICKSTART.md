# 快速开始指南

## 5分钟快速体验

### 步骤1：准备腾讯云LKE智能体

1. 登录 [腾讯云LKE平台](https://console.cloud.tencent.com/lke)
2. 创建新的智能体应用
3. 在"系统提示词"中粘贴以下内容：

```
# 中国象棋AI助手

你是一个专业的中国象棋AI对弈助手。你的任务是根据当前棋盘状态，选择最佳走子并以规定格式输出。

## 输出格式要求
**极其重要**：你必须严格按照以下格式输出走子指令：

格式：`MOVE: 起始行起始列-目标行目标列`

示例：
- `MOVE: 01-03` （将位置(0,1)的棋子移动到(0,3)）
- `MOVE: 27-47` （将位置(2,7)的炮移动到(4,7)）

### 完整回答格式
```
[简要分析当前局面]

我选择的走子是：
MOVE: 起始行起始列-目标行目标列

[简要说明走子理由]
```

## 走子规则
1. 将/帅：只能在九宫格内移动，每次一格
2. 士：只能在九宫格内斜走，每次一格
3. 象/相：走田字，不能过河，象眼不能被堵
4. 马：走日字，马腿不能被堵
5. 车：直线移动，路径不能有障碍
6. 炮：直线移动，吃子时必须隔一个棋子
7. 兵/卒：未过河只能向前，过河后可以左右移动

记住：每次回答都必须包含一个明确的MOVE指令！
```

4. 保存并记录以下信息：
   - BotBizID（智能体业务ID）
   - AppID（应用ID）

### 步骤2：获取腾讯云密钥

1. 访问 [腾讯云API密钥管理](https://console.cloud.tencent.com/cam/capi)
2. 创建或查看现有密钥
3. 记录：
   - SecretId
   - SecretKey

### 步骤3：配置项目

```bash
# 进入项目目录
cd /Users/gongfeng/AI

# 复制配置文件
cp .env.example .env

# 编辑配置文件
vim .env
```

填入你的配置：
```env
TENCENT_SECRET_ID=你的SecretId
TENCENT_SECRET_KEY=你的SecretKey
TENCENT_REGION=ap-guangzhou
LKE_BOT_BIZ_ID=你的BotBizID
LKE_APP_ID=你的AppID
PORT=8080
```

### 步骤4：启动游戏

```bash
# 方式1：使用启动脚本（推荐）
./start.sh

# 方式2：直接运行
go run main.go
```

### 步骤5：开始游戏

1. 打开浏览器访问：`http://localhost:8080`
2. 选择你的颜色（红方或黑方）
3. 点击"开始新游戏"
4. 开始对弈！

## 游戏操作说明

### 如何走子
1. 点击你要移动的棋子（会显示绿色选中框）
2. 点击目标位置
3. 如果移动合法，棋子会移动到目标位置
4. AI会自动思考并走子

### 游戏界面说明
- **当前回合**：显示现在是哪方走子
- **游戏状态**：显示游戏进行状态
- **AI分析**：显示AI的思考过程和走子理由
- **走子历史**：显示所有历史走子记录

## 常见问题

### Q1: 启动失败，提示"创建LKE客户端失败"
**解决方案**：
- 检查.env文件中的配置是否正确
- 确认SecretId和SecretKey是否有效
- 检查网络连接是否正常

### Q2: AI走子很慢
**解决方案**：
- LKE智能体首次调用可能较慢，后续会加快
- 检查网络延迟
- 可以在LKE平台调整模型参数

### Q3: AI走子不合法
**解决方案**：
- 检查LKE智能体的系统提示词是否正确配置
- 确保提示词中强调了输出格式要求
- 查看后端日志中的"LKE响应"，分析AI的原始输出

### Q4: 前端无法加载
**解决方案**：
- 检查后端是否正常启动
- 确认端口8080没有被占用
- 查看浏览器控制台的错误信息

### Q5: 如何提升AI水平
**解决方案**：
- 优化系统提示词，添加更多策略指导
- 在LKE平台选择更强大的模型
- 添加开局库和残局库

## 测试验证

### 运行单元测试
```bash
# 测试象棋逻辑
go test ./internal/chess -v

# 测试所有模块
go test ./... -v
```

### 手动测试流程
1. 创建新游戏 ✓
2. 玩家走子 ✓
3. AI响应 ✓
4. 查看历史 ✓
5. 游戏结束判定 ✓

## 下一步

### 功能扩展
- [ ] 添加悔棋功能
- [ ] 实现对局保存和加载
- [ ] 添加难度等级选择
- [ ] 支持双人对战模式
- [ ] 添加开局库和残局库
- [ ] 实现对局回放功能

### 性能优化
- [ ] 添加Redis缓存
- [ ] 实现连接池
- [ ] 优化前端渲染
- [ ] 添加CDN加速

### 部署上线
- [ ] 配置HTTPS
- [ ] 添加域名
- [ ] 配置监控告警
- [ ] 实现自动备份

## 获取帮助

- 查看 [README.md](README.md) 了解详细文档
- 查看 [PROMPT_TEMPLATE.md](PROMPT_TEMPLATE.md) 了解提示词优化
- 查看 [DEPLOYMENT.md](DEPLOYMENT.md) 了解部署方案
- 提交Issue报告问题

## 示例对局

### 开局示例
```
1. 红方：兵64-54 (中兵进一)
2. 黑方：MOVE: 34-44 (卒进一)
3. 红方：马71-63 (马跳)
4. 黑方：MOVE: 01-21 (马跳)
...
```

## 技术支持

如遇到问题，请提供以下信息：
1. 错误信息截图
2. 后端日志（app.log）
3. 浏览器控制台错误
4. 操作步骤描述

祝你游戏愉快！🎮
