# 会话式AI对话优化说明

## 问题描述

之前每次AI走子时，都会发送完整的棋盘状态、规则说明等大量信息，导致：
- ❌ 每次请求的token消耗过大
- ❌ AI需要重复处理相同的规则信息
- ❌ 没有充分利用LKE的会话机制

## 优化方案

### 核心思路

利用LKE的会话机制，将对话分为两部分：

1. **系统提示词（只发送一次）**
   - 在LKE平台配置
   - 包含完整的游戏规则、坐标系统、输出格式要求
   - 参考：[LKE_SYSTEM_PROMPT_OPTIMIZED.md](LKE_SYSTEM_PROMPT_OPTIMIZED.md)

2. **用户消息（每次走子）**
   - 只发送本次的走子信息
   - 简洁明了，减少token消耗
   - AI通过会话上下文理解整个对局

### 代码修改

#### 修改文件：`internal/game/manager.go`

**修改前（每次发送完整信息）：**
```go
prompt := fmt.Sprintf(`# 当前对局状态

## 棋盘布局（FEN格式）
%s

## 当前棋盘可视化
%s

## 对局信息
- 当前轮到：%s
- 你执：%s
- 历史走子：%s

## 你的任务
请分析当前局面，选择一个合法的走子。
...（大量规则说明）
`, ...)
```

**修改后（只发送本次走子）：**
```go
var prompt string
if len(g.Board.MoveList) == 0 {
    // 第一步，AI先手
    prompt = "游戏开始，你是先手，请走第一步。"
} else {
    // 告诉AI对手刚才的走子
    lastMove := g.Board.MoveList[len(g.Board.MoveList)-1]
    prompt = fmt.Sprintf("对手走了：%s，现在轮到你了，请走子。", lastMove)
}
```

## 优化效果

### Token消耗对比

| 场景 | 优化前 | 优化后 | 节省 |
|------|--------|--------|------|
| 首次走子 | ~800 tokens | ~20 tokens | **97.5%** |
| 后续走子 | ~800 tokens | ~30 tokens | **96.3%** |

### 对话示例

**第1回合（AI先手）：**
```
用户: 游戏开始，你是先手，请走第一步。
AI: 炮二平五
```

**第2回合（玩家走子后）：**
```
用户: 对手走了：炮8平5，现在轮到你了，请走子。
AI: 马二进三
```

**第3回合：**
```
用户: 对手走了：马8进7，现在轮到你了，请走子。
AI: 车一平二
```

## 前置条件

### 必须在LKE平台配置系统提示词

系统提示词包含：
1. ✅ 游戏规则说明
2. ✅ 坐标系统定义
3. ✅ 输出格式要求
4. ✅ 棋盘初始状态
5. ✅ 走子记录格式

详细内容参考：[LKE_SYSTEM_PROMPT_OPTIMIZED.md](LKE_SYSTEM_PROMPT_OPTIMIZED.md)

### 配置步骤

1. 登录腾讯云LKE平台
2. 找到你的智能体配置
3. 在"系统提示词"或"System Prompt"中粘贴优化后的提示词
4. 保存配置

## 测试验证

### 方法1：使用测试脚本

```bash
# 重新编译和启动服务
./start.sh

# 运行测试脚本
./test_session_chat.sh
```

测试脚本会：
1. 创建新游戏
2. 玩家走子
3. AI走子
4. 显示发送给AI的提示词
5. 验证提示词是否简洁

### 方法2：查看日志

```bash
# 实时查看日志
tail -f logs/chinese-chess-ai.log

# 查找发送给AI的提示词
grep "发送提示词给AI" logs/chinese-chess-ai.log -A 2
```

### 预期结果

✅ **优化成功的标志：**
```
[Game] 发送提示词给AI: 游戏开始，你是先手，请走第一步。
[Game] 发送提示词给AI: 对手走了：炮二平五，现在轮到你了，请走子。
[Game] 发送提示词给AI: 对手走了：马二进三，现在轮到你了，请走子。
```

❌ **优化未生效的标志：**
```
[Game] 发送提示词给AI:
# 当前对局状态
## 棋盘布局（FEN格式）
...（大量内容）
```

## 注意事项

### 1. 会话管理

- ✅ 每个游戏使用独立的会话ID
- ✅ 会话ID在游戏创建时生成
- ✅ 整个对局过程使用同一个会话ID

### 2. 走子格式

走子使用中文棋谱格式，例如：
- `炮二平五` - 红方炮从第2路平移到第5路
- `马8进7` - 黑方马从第8路进到第7路
- `车一进一` - 红方车从第1路前进1步

### 3. 系统提示词更新

如果修改了系统提示词：
1. 在LKE平台更新配置
2. 创建新游戏测试（使用新会话）
3. 旧游戏仍使用旧的系统提示词

## 故障排查

### 问题1：AI仍然收到大量信息

**原因：** 代码未重新编译

**解决：**
```bash
./start.sh  # 会自动重新编译
```

### 问题2：AI无法理解走子

**原因：** 系统提示词未配置或配置不正确

**解决：**
1. 检查LKE平台的系统提示词配置
2. 确保包含完整的规则说明
3. 参考 [LKE_SYSTEM_PROMPT_OPTIMIZED.md](LKE_SYSTEM_PROMPT_OPTIMIZED.md)

### 问题3：AI输出格式错误

**原因：** 系统提示词中的格式要求不够明确

**解决：**
1. 在系统提示词中强调输出格式
2. 提供具体的示例
3. 使用"必须"、"严格"等强调词

## 相关文档

- [LKE_SYSTEM_PROMPT_OPTIMIZED.md](LKE_SYSTEM_PROMPT_OPTIMIZED.md) - 优化后的系统提示词
- [test_session_chat.sh](test_session_chat.sh) - 会话式对话测试脚本
- [AI_INTERACTION_FIX.md](AI_INTERACTION_FIX.md) - AI交互修复说明
- [DEBUG_GUIDE.md](DEBUG_GUIDE.md) - 调试指南

## 总结

通过这次优化：

✅ **大幅减少token消耗**（节省95%以上）
✅ **提高响应速度**（减少处理时间）
✅ **更符合对话习惯**（简洁自然）
✅ **充分利用会话机制**（上下文连贯）

这是一个典型的"系统提示词 + 简洁用户消息"的最佳实践！
